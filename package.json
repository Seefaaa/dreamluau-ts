{
  "name": "dreamluau-ts",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter scripts build",
    "build:formatter": "bun run --filter formatter build",
    "build:linter": "bun run --filter blocking-lint build",
    "dev": "bun run --filter scripts dev",
    "test": "bun test",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "check": "biome check .",
    "check:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5"
  }
}
